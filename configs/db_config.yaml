# 데이터베이스 접속 설정
# 실제 값은 .env 파일에서 로드됩니다
# .env 파일을 생성하고 DB_HOST, DB_PORT, DB_USER, DB_PASSWORD, DB_NAME을 설정하세요
aws_rds:
  host: "${DB_HOST}"
  port: ${DB_PORT}
  user: "${DB_USER}"
  password: "${DB_PASSWORD}"
  database: "${DB_NAME}"